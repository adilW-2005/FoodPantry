# 🔄 SINGLE TOGGLE: Change this to switch between development/production
ENVIRONMENT=production

# Production-only variables (not needed in development)
SECRET_KEY=your-django-secret-key-here
ALLOWED_HOSTS=your-app-name.onrender.com
DATABASE_URL=postgresql://username:password@hostname:port/database_name
FRONTEND_URL=https://your-vercel-app.vercel.app

# AWS S3 Configuration (used in both environments)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_STORAGE_BUCKET_NAME=your-s3-bucket-name

# Optional: Additional security settings (automatically applied in production)
# These are handled automatically based on ENVIRONMENT variable 